# Akasha Astrology Engine — State

## Goal
Astrology engine (bazi, vedic, western, ziwei) with synthesis layer, UI, and i18n. 20 todos in `.omo/plans/akasha-astrology-engine.md`.

## Current State
- **PLAN COMPLETE (2026-08-08)**: `.omo/plans/akasha-astrology-engine.md` — 20/20 todos [x] + Final Wave F1–F4 all APPROVE. F3 (Manual QA) unblocked via BrowserClaw raw MCP: browser QA nyata (form → dashboard "Peta Kelahiran" NatalChart panel → Report LuckCycle 大運) + live URL HTTP 200. 295/295 tests, tsc 0, line-length 0, build hijau.
- **DEPLOYED LIVE**: https://celekngasu52-cyber.github.io/akasha/ — GH Pages + Actions workflow green (run id 31200867017, build+deploy success).
- Todo 17 (svg charts) + 18 (PWA) + 20 (CI deploy) done. InputPage utils refactor + CSP landed `6aa8c4f`.
- Harian tab renders a 7-day forecast list (today "Hari ini" + Besok/Lusa/weekday, date label, 4 domain scores, tlDr) — via `buildDailyForecast`.
- **REAL PIPELINE (dashboard-mock DELETED)**: `src/pages/dashboard-data.ts` replaces FNV-random mock. Deterministic, birth-driven, sync BaZi only: natal 5-element tally from computeFourPillars blended 0.6/0.4 with real elementScores from computeDailyForecast/Weekly/Monthly/Yearly. Domain→element affinity (Karier→fire, Cinta→water, Kesehatan→wood, Keuangan→earth). Same export names (buildDashboardData/buildDailyForecast/DASHBOARD_DOMAINS/DAILY_HORIZON_DAYS/DailyEntry) now take `birthData`. Dashboard.tsx + Report.tsx pass birthData.
- **Gender input added**: `BirthData.gender?: 'male'|'female'` (optional, engines+tests unmapped) + "Jenis Kelamin" radio in InputPage (optional). Pipeline resolves 0/1 with male default.
- **Em-dash AI-slop cleaned** in Dashboard.tsx/Report.tsx visible copy (→ `·`, `:`, `(label)`). narrative.ts template dashes untouched (synthesis lock).
- **PREMIUM: "Laporan Lengkap" report** (`src/pages/Report.tsx`) — printable PDF doc: real BaZi natal (computeFourPillars+computeStrength+computeTenGods, sync/no gender), 7-day forecast, 4-system summary, Metodologi & Glosarium appendix; "Cetak / Simpan PDF" via window.print() + inline @media print CSS (no deps); profile saved to localStorage (`akasha:latest-profile`); route 'report' in App.tsx + `onOpenReport` prop on Dashboard.
- **PREMIUM: "Koleksi" multi-profil** (`src/lib/profileStore.ts` + `src/pages/Collection.tsx`) — localStorage profile library (`akasha:profiles`), dedupe by birth signature (`profileKey`), save from Dashboard ("Simpan" button), open/delete per card, route 'collection' + `onOpenCollection` prop.
- **PREMIUM: "Jodoh" compatibility** (`src/lib/compatibility.ts` + `src/pages/Compatibility.tsx`) — two-person BaZi compatibility: day-master element relation (相生/相克/seunsur via STEM_ELEMENT + GENERATES/CONTROLS), 六合 (+12)/六冲 (-12) day-branch adjustment, baseScore 78/74/60/45 + deterministic FNV-1a jitter ±3; per-domain scores seeded `compat|key|domain` ±8; tone harmonis/netral/menantang; pure & deterministic (`compatibilityKey` order-insensitive); form for person B (reuses input-utils getCities/filterCities/validateDate/validateTime, no react-router); route 'compat' + `onOpenCompatibility` prop + "Jodoh" button.
- Local: 265 tests passing, `npm run build` succeeds.

## Decisions
- cities.json uses `_meta` key (not JSONC) for source documentation
- React component tests use `react-dom/server` (no jsdom, can't add deps)
- axe-core deferred (not installed) — manual a11y review
- Dashboard routing: `useState<Route>` in App.tsx (no react-router dep)
- WhyPanel: collapsible per-card (not bottom sheet) — keeps reasoning local
- Mock data: FNV-1a + mulberry32, seeded by `${horizon}|${domain}`; shapes mirror scorer.DomainScore/EngineDetail + narrative.buildTlDr. Swap = single import change (todo 17/19).
- TrendChart: hand-rolled SVG (polyline + area + gridline), `dangerouslySetInnerHTML` for endpoint circles.
- **Deploy creds**: remote URL embeds fine-grained PAT `akasha-deploy-cli` (Contents+Workflows RW, 1 repo, exp 2026-09-06). Old PAT in remote (no workflow scope) replaced. Pages source = GitHub Actions (enabled via browser).
- **BrowserClaw MCP (browseros-neo) is down at opencode client level** — raw MCP HTTP API works. Helper: `C:\Users\Dani\AppData\Local\Temp\opencode\bc-mcp.mjs` (`node bc-mcp.mjs <tool> @args.json [timeout]`), args from JSON file (PowerShell strips quotes otherwise).

## Constraints
- MUST NOT add new npm deps
- MUST NOT modify src/engines/**, src/synthesis/**, src/theme.css, src/components/ui.tsx
- Line-length limit: 100 chars (check-line-length.mjs)
- tsconfig: verbatimModuleSyntax, erasableSyntaxOnly, noUnusedLocals/Params
- CI: workflow needs Pages source = GitHub Actions (already set)

## Important Files
- BirthData type: `src/core/birth/types.ts`
- Birth barrel: `src/core/birth.ts`
- Design system: `src/theme.css` (Tailwind v4 @theme → --aka-* tokens)
- UI primitives: `src/components/ui.tsx` (Button, Card, Section, Badge, Tabs, Modal, Skeleton)
- Dashboard: `src/pages/Dashboard.tsx` (harian tab = 7-day list via `buildDailyForecast`; other tabs = 4 DomainCards) + `src/pages/dashboard-mock.ts` (exports `DAILY_HORIZON_DAYS`, `DailyEntry`, `buildDailyForecast`)
- Report: `src/pages/Report.tsx` (printable "Laporan Lengkap", real BaZi natal + print CSS, localStorage profile)
- Koleksi: `src/lib/profileStore.ts` (localStorage `akasha:profiles`, profileKey/upsert/remove) + `src/pages/Collection.tsx`
- Compatibility: `src/lib/compatibility.ts` (computeCompatibility/compatibilityKey/elementOfStem) + `src/pages/Compatibility.tsx` (person-B form + verdict)
- Components: `src/components/{DomainCard,TrendChart,WhyPanel}.tsx`
- Routing: `src/App.tsx` (useState input→dashboard→report/compat/collection)
- InputPage utils: `src/pages/input-utils.ts` (validateDate/Time, filterCities, getCities)
- Dashboard data: `src/pages/dashboard-data.ts` (REAL sync BaZi pipeline; `dashboard-mock.ts` deleted; Builders take `BirthData`, gender optional)
- BirthData: `src/core/birth/types.ts` (has optional `gender?: 'male'|'female'`)
- Deploy: `.github/workflows/deploy.yml` (push→main builds with base '/akasha/' → GH Pages)

## Commands/Tests
- `npx vitest run` — all tests (266 passing)
- `node check-line-length.mjs` — line-length check
- `npx vite build` — production build (succeeds)
- Deploy: commit → `git push origin main` → watch run → site live at /akasha/

## Blockers
- BrowserClaw MCP client broken (tabs/act all throw "Cannot read properties of undefined"); **WORKAROUND WORKS (2026-08-08)**: raw MCP streamable-HTTP via `C:\Users\Dani\AppData\Local\Temp\opencode\bc-mcp.mjs` — handshake initialize → persist `Mcp-Session-Id` ke `bc-session.json` (hapus file dulu jika "Session not found") → panggil `tools/call` dengan `{name, arguments}` (bukan nama tool sebagai method); params via stdin (PowerShell strip quote JSON); evaluate butuh `return` eksplisit di top level (bukan IIFE). agent-browser daemon juga dead.
- PAT `akasha-deploy` deletion blocked by GitHub sudo-mode ("Confirm access" via email) — page left open in BrowserOS neo session (tab 14, token id 17973163). User must click "Verify via email" + open email link, then re-run delete.
- Remote URL cleaned: `git remote set-url origin https://github.com/celekngasu52-cyber/akasha.git` (PAT was embedded). Pushes now via gh credential helper (`gh auth setup-git`, active account softhubmanagement-afk). Second token `akasha-deploy-cli` left untouched.

## Next Steps
- Wire the FULL async Chart4 → scorer → narrative (vedic/western are async swisseph-Wasm: computeVedicChart/computeNatalChart return Promises; needs loading/error state in Dashboard, bigger refactor). Scorer/scoreAll already accepts Chart4.
- Finish deleting PAT `akasha-deploy` (needs user sudo-email verify first). Decide on `akasha-deploy-cli`.
