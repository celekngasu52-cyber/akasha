# Akasha Astrology Engine — State

## Goal
Astrology engine (bazi, vedic, western, ziwei) with synthesis layer, UI, and i18n. 20 todos in `.omo/plans/akasha-astrology-engine.md`.

## Current State
- Todo 16 (Dashboard) COMPLETE — committed `122ab93`.
- Todo 15 (InputPage) + 14 (theme) done previously.
- tsc-fix landed (`02498bb`) — engines/synthesis clean.

## Decisions
- cities.json uses `_meta` key (not JSONC) for source documentation
- React component tests use `react-dom/server` (no jsdom, can't add deps)
- axe-core deferred (not installed) — manual a11y review
- Dashboard routing: `useState<Route>` in App.tsx (no react-router dep)
- WhyPanel: collapsible per-card (not bottom sheet) — keeps reasoning local
- Mock data: FNV-1a + mulberry32, seeded by `${horizon}|${domain}`; shapes mirror scorer.DomainScore/EngineDetail + narrative.buildTlDr. Swap = single import change (todo 17/19).
- TrendChart: hand-rolled SVG (polyline + area + gridline), `dangerouslySetInnerHTML` for endpoint circles.

## Constraints
- MUST NOT add new npm deps
- MUST NOT modify src/engines/**, src/synthesis/**, src/theme.css, src/components/ui.tsx
- Line-length limit: 100 chars (check-line-length.mjs)
- tsconfig: verbatimModuleSyntax, erasableSyntaxOnly, noUnusedLocals/Params

## Important Files
- BirthData type: `src/core/birth/types.ts`
- Birth barrel: `src/core/birth.ts`
- Design system: `src/theme.css` (Tailwind v4 @theme → --aka-* tokens)
- UI primitives: `src/components/ui.tsx` (Button, Card, Section, Badge, Tabs, Modal, Skeleton)
- Dashboard: `src/pages/Dashboard.tsx` + `src/pages/dashboard-mock.ts`
- Components: `src/components/{DomainCard,TrendChart,WhyPanel}.tsx`
- Routing: `src/App.tsx` (useState input→dashboard)
- Tests: `src/pages/__tests__/Dashboard.test.tsx` (6 tests)

## Commands/Tests
- `npx vitest run` — all tests (227 passing)
- `npx vitest run src/pages/__tests__/Dashboard.test.tsx` — Dashboard only (6 tests)
- `node check-line-length.mjs` — line-length check
- `npx vite build` — production build (succeeds)
- `npx vite preview --port 4173` — preview built dist

## Blockers
- BrowserClaw MCP down: 406 on port 9010 health check; all tabs/navigate/run calls fail. Dashboard screenshot AC unmet — substituted with react-dom/server assertions.

## Next Steps
- Todo 17 (result page) — wire real Chart4→scorer→narrative pipeline, replace dashboard-mock import.
- Todo 18 (PWA) depends on 16+17.
