# Akasha Astrology Engine — State

## Goal
Astrology engine (bazi, vedic, western, ziwei) with synthesis layer, UI, and i18n. 20 todos in `.omo/plans/akasha-astrology-engine.md`.

## Current State
- **DEPLOYED LIVE**: https://celekngasu52-cyber.github.io/akasha/ — GH Pages + Actions workflow green (run id 31200867017, build+deploy success).
- Todo 17 (svg charts) + 18 (PWA) + 20 (CI deploy) done. InputPage utils refactor + CSP landed `6aa8c4f`.
- Harian tab now renders a 7-day forecast list (today "Hari ini" + Besok/Lusa/weekday, each with date label, 4 domain scores, tlDr) — added `buildDailyForecast` to dashboard-mock.ts.
- Local: 238 tests passing, `npm run build` succeeds.

## Decisions
- cities.json uses `_meta` key (not JSONC) for source documentation
- React component tests use `react-dom/server` (no jsdom, can't add deps)
- axe-core deferred (not installed) — manual a11y review
- Dashboard routing: `useState<Route>` in App.tsx (no react-router dep)
- WhyPanel: collapsible per-card (not bottom sheet) — keeps reasoning local
- Mock data: FNV-1a + mulberry32, seeded by `${horizon}|${domain}`; shapes mirror scorer.DomainScore/EngineDetail + narrative.buildTlDr. Swap = single import change (todo 17/19).
- TrendChart: hand-rolled SVG (polyline + area + gridline), `dangerouslySetInnerHTML` for endpoint circles.
- **Deploy creds**: remote URL embeds fine-grained PAT `akasha-deploy-cli` (Contents+Workflows RW, 1 repo, exp 2026-09-06). Old PAT in remote (no workflow scope) replaced. Pages source = GitHub Actions (enabled via browser).
- **BrowserClaw MCP (browseros-neo) is down at opencode client level** — raw MCP HTTP API works. Helper: `C:\Users\Dani\AppData\Local\Temp\opencode\bc-mcp.mjs` (`node bc-mcp.mjs <tool> @args.json [timeout]`), args from JSON file (PowerShell strips quotes otherwise).

## Constraints
- MUST NOT add new npm deps
- MUST NOT modify src/engines/**, src/synthesis/**, src/theme.css, src/components/ui.tsx
- Line-length limit: 100 chars (check-line-length.mjs)
- tsconfig: verbatimModuleSyntax, erasableSyntaxOnly, noUnusedLocals/Params
- CI: workflow needs Pages source = GitHub Actions (already set)

## Important Files
- BirthData type: `src/core/birth/types.ts`
- Birth barrel: `src/core/birth.ts`
- Design system: `src/theme.css` (Tailwind v4 @theme → --aka-* tokens)
- UI primitives: `src/components/ui.tsx` (Button, Card, Section, Badge, Tabs, Modal, Skeleton)
- Dashboard: `src/pages/Dashboard.tsx` (harian tab = 7-day list via `buildDailyForecast`; other tabs = 4 DomainCards) + `src/pages/dashboard-mock.ts` (exports `DAILY_HORIZON_DAYS`, `DailyEntry`, `buildDailyForecast`)
- Components: `src/components/{DomainCard,TrendChart,WhyPanel}.tsx`
- Routing: `src/App.tsx` (useState input→dashboard)
- InputPage utils: `src/pages/input-utils.ts` (validateDate/Time, filterCities, getCities)
- Deploy: `.github/workflows/deploy.yml` (push→main builds with base '/akasha/' → GH Pages)

## Commands/Tests
- `npx vitest run` — all tests (238 passing)
- `node check-line-length.mjs` — line-length check
- `npx vite build` — production build (succeeds)
- Deploy: commit → `git push origin main` → watch run → site live at /akasha/

## Blockers
- BrowserClaw MCP client broken (tabs/act all throw "Cannot read properties of undefined"); workaround = raw MCP HTTP via bc-mcp.mjs. agent-browser daemon also dead.

## Next Steps
- Replace dashboard-mock with real Chart4→scorer→narrative pipeline (todo 19/21).
- Consider deleting old workflow-scope-less PAT ("akasha-deploy") from GitHub settings.
